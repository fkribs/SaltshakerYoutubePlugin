{
  "name": "@saltshaker/plugin-youtube",
  "version": "1.0.0",
  "description": "Saltshaker plugin for YouTube watch-together.",
  "type": "commonjs",
  "main": "dist/index.js",
  "files": [
    "dist",
    "manifest.json"
  ],
  "scripts": {
    "clean": "rimraf dist && rimraf -g \"*.tgz\"",
    "build": "esbuild src/index.js --bundle --platform=node --target=node18 --outfile=dist/index.js",
    "bundle": "npm run clean && npm run build && npm pack"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "rimraf": "^6.0.0"
  }
}
